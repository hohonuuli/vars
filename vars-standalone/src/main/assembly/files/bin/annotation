#!/bin/sh

source `dirname "$0"`/varsSetup

${JAVACMD:=java} -Xms16m -Xmx512m \
         -Duser.timezone="UTC" \
         -Dfile.encoding=UTF8 \
         -classpath "$VARS_CLASSPATH" \
         vars.annotation.ui.AnnotationApp "$@"